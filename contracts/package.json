{
  "name": "sol-ai-contracts",
  "version": "1.0.0",
  "description": "Enhanced SOL AI Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network somnia",
    "deploy-enhanced": "hardhat run deploy.js --network somnia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}
